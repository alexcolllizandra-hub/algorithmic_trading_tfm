# =====================================================================
# DEVELOPMENT-DATA PILOT (ETHUSDT) — verified REAL historical data.
# Small matched Random Search vs Genetic Algorithm comparison on the
# ETHUSDT 1h validated development partition (never the frozen holdout).
# EXPLORATORY pipeline-validation pilot, NOT thesis evidence.
# =====================================================================

experiment_config: configs/experiment.yaml
data_contract: configs/data_contract.yaml

family: momentum
algorithm: comparison
symbol: ETHUSDT
timeframe: "1h"
seed: 42
regime_model: threshold

require_funding: true
synthetic: false
label: development_pilot_ETHUSDT_EXPLORATORY_not_holdout

max_folds: 3

ga:
  population_size: 4
  generations: 3
  crossover_rate: 0.7
  mutation_rate: 0.3
  elitism: 1
  tournament_size: 3
